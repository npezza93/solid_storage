<%= link_to "add", new_post_path %>

<% @posts.each do |post| %>
  <% if post.file.image? %>
    <%= image_tag post.file, style: "width: 50px;height: 50px;" %>
  <% else %>
    <%= video_tag post.file, style: "width: 500px;height: 400px;" %>
  <% end %>
<% end %>
